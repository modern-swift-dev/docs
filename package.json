{
  "name": "modern-swift-docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:content": "node --experimental-strip-types scripts/prepare-content.ts",
    "build:api": "node --experimental-strip-types scripts/build-api.ts",
    "check": "astro check",
    "build:guides": "astro build",
    "assemble": "node --experimental-strip-types scripts/assemble.ts",
    "check:links": "node --experimental-strip-types scripts/check-links.ts",
    "build": "npm run prepare:content && npm run check && npm run build:guides && npm run assemble && npm run check:links",
    "preview": "python3 scripts/preview.py"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@tailwindcss/vite": "^4.1.0",
    "astro": "^7.0.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
